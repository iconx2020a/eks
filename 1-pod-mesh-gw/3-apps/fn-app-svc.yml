---
kind: Pod
apiVersion: v1
metadata:
  name: fnapp
  labels:
    app: fnapp
spec:
  containers:
    - name: fnapp
      image: iconx2020a/eks:fnapp-green
      imagePullPolicy: Always
      ports:
        - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: fnapp-svc
spec:
  selector:
    app: fnapp
  ports:
  - protocol: "TCP"
    port: 8080
    targetPort: 8080
